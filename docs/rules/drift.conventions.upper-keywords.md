# drift.conventions.upper-keywords

**uppercase keywords** .  category: `conventions`  severity: `info`  fixable: false

run `drift explain drift.conventions.upper-keywords` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "conventions" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.conventions.upper-keywords" = "error"     # or warning, info, off
```


