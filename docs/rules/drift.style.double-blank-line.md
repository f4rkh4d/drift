# drift.style.double-blank-line

**double blank line** .  category: `style`  severity: `info`  fixable: false

run `drift explain drift.style.double-blank-line` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "style" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.style.double-blank-line" = "error"     # or warning, info, off
```


