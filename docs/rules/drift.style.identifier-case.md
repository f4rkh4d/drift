# drift.style.identifier-case

**identifier case**  —  category: `style`  severity: `info`  fixable: false

run `drift explain drift.style.identifier-case` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "style" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.style.identifier-case" = "error"     # or warning, info, off
```


