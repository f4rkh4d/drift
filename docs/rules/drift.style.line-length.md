# drift.style.line-length

**line length** .  category: `style`  severity: `info`  fixable: false

run `drift explain drift.style.line-length` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "style" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.style.line-length" = "error"     # or warning, info, off
```


