# drift.style.space-after-comma

**space after comma**  —  category: `style`  severity: `info`  fixable: false

run `drift explain drift.style.space-after-comma` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "style" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.style.space-after-comma" = "error"     # or warning, info, off
```


