# drift.style.space-around-operator

**space around operator** .  category: `style`  severity: `info`  fixable: false

run `drift explain drift.style.space-around-operator` for the interactive version.

## why

see the rule description in `drift explain`. short version: this is a "style" rule, fires at `info` by default.

## configuration

```toml
[rules]
"drift.style.space-around-operator" = "error"     # or warning, info, off
```


